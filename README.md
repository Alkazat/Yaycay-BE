# Yaycay Back-End

The platform core for Yaycay (API, data + RLS, AI harness, MCP, Stripe, Brevo). This thread owns the shared contract `@yaycay/contracts`.

Project scaffold and Phase 0 land via PR. See `docs/` for the model context and backend handoff.
